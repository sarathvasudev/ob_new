//Enums are available through oicl library imports. I believe we can reuse those.
//So commenting out the enums here.

/* export enum PropellerType{
    none='none',
    cap ='cap',
    single = 'single',
}

export enum AdviceType {
  advice = 'advice',
  caution = 'caution',
}

export enum InstrumentState {
  inCommand = 'advice',
  active = 'active',
  loading = 'loading',
  off = 'off',
} */

